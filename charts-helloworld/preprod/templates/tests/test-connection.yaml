apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "preprod.fullname" . }}-test-connection"
  labels:
    {{- include "preprod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "preprod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
