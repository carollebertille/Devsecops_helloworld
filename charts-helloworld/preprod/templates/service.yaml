apiVersion: v1
kind: Service
metadata:
  name: {{ include "preprod.fullname" . }}
  labels:
    {{- include "preprod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "preprod.selectorLabels" . | nindent 4 }}
